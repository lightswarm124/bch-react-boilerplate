name: Local Runner Test

on:
  push:
    branches:
      - main
      - master
      - dev
      - "*" # catch-all safety

jobs:
  test-runner:
    runs-on: [self-hosted, ubuntu]
    steps:
      - name: Show runner info
        run: |
          echo "Hello from $(hostname)"
          uname -a
