name: Local Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, ubuntu]
    steps:
      - name: Check runner info
        run: |
          echo "Running on $(hostname)"
          uname -a
